import { NgIfMediaQueryDirective } from './ng-if-media-query.directive';

describe('NgIfMediaQueryDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfMediaQueryDirective();
    expect(directive).toBeTruthy();
  });
});
